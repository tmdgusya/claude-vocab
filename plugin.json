{
  "name": "claude-vocab",
  "version": "1.0.0",
  "description": "Auto-export and parse your Claude Code conversation transcripts. Extracts only user prompts from sessions.",
  "author": "tmdgusya",
  "homepage": "https://github.com/tmdgusya/claude-vocab",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/export-transcript.sh"
          }
        ]
      }
    ]
  },
  "commands": [
    {
      "name": "export-prompts",
      "description": "Manually export user prompts from the current or a specific session",
      "source": "${CLAUDE_PLUGIN_ROOT}/commands/export-prompts.md"
    },
    {
      "name": "list-exports",
      "description": "List all exported transcripts",
      "source": "${CLAUDE_PLUGIN_ROOT}/commands/list-exports.md"
    }
  ]
}
